import { IconFolder } from "@tabler/icons-react"

// 프로젝트 지출 배지
export function ProjectBadge() {
  return (
    <span data-badge aria-label="프로젝트">
      <IconFolder size={12} aria-hidden />
    </span>
  )
}
